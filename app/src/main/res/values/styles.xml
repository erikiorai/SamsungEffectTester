<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="ActivityTheme" parent="android:Theme.DeviceDefault.NoActionBar.Fullscreen" >
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>

        <item name="android:windowActivityTransitions">true</item>
        <item name="android:windowEnterTransition">@android:anim/fade_in</item>
        <item name="android:windowExitTransition">@android:anim/fade_out</item>
        <item name="android:windowSharedElementEnterTransition">@android:anim/fade_in</item>
        <item name="android:windowSharedElementExitTransition">@android:anim/fade_out</item>
    </style>
    <style name="Theme.ActionBar" parent="@android:style/Widget.DeviceDefault.ActionBar">
        <item name="android:displayOptions">showTitle</item>
        <item name="android:contentInsetStart">@dimen/actionbar_contentInsetStart</item>
    </style>
    <style name="Theme.DialogWhenLarge" parent="android:Theme.DeviceDefault.DialogWhenLarge">
        <!--item name="android:colorBackground">@android:color/white</item-->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:actionBarStyle">@style/Theme.ActionBar</item>

        <item name="android:windowActivityTransitions">true</item>
        <item name="android:windowEnterTransition">@android:transition/no_transition</item>
        <item name="android:windowExitTransition">@android:transition/no_transition</item>
        <item name="android:windowSharedElementEnterTransition">@android:transition/no_transition</item>
        <item name="android:windowSharedElementExitTransition">@android:transition/no_transition</item>

        <!--item name="preferenceBackgroundColor">@drawable/preference_background</item>-->
    </style>
    <style name="keyguard_text_shadow" parent="">
        <item name="android:shadowDy">2</item>
        <item name="android:shadowRadius">2</item>
    </style>
    <style name="list_title_text_style">
        <item name="android:textAppearance">?android:attr/textAppearanceListItem</item>
    </style>
    <dimen name="actionbar_contentInsetStart">16dp</dimen>
</resources>